name: Build Regime Mobile APK

on:
  push:
    branches: [regime-mobile-build]
    paths:
      - 'regime-mobile/**'
      - '.github/workflows/regime-mobile-apk.yml'
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'
      - uses: android-actions/setup-android@v3
      - name: Install Android SDK
        run: sdkmanager 'platforms;android-35' 'build-tools;35.0.0'
      - uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.9'
      - name: Build debug APK
        working-directory: regime-mobile
        run: gradle :app:assembleDebug --no-daemon --stacktrace
      - name: Rename APK
        run: cp regime-mobile/app/build/outputs/apk/debug/app-debug.apk regime-mobile/Regime-Mobile-1.0.0.apk
      - uses: actions/upload-artifact@v4
        with:
          name: Regime-Mobile-APK
          path: regime-mobile/Regime-Mobile-1.0.0.apk
          if-no-files-found: error
